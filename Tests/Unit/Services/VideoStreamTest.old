<?php

declare(strict_types=1);

namespace Tests\Unit\Modules\Media\Services;

use Modules\Media\Services\VideoStream;
use Tests\TestCase;

/**
 * Class VideoStreamTest.
 *
 * @covers \Modules\Media\Services\VideoStream
 */
final class VideoStreamTest extends TestCase
{
    private VideoStream $videoStream;

    private string $disk;

    private string $path;

    /**
     * {@inheritdoc}
     */
    protected function setUp(): void
    {
        parent::setUp();

        $this->disk = '42';
        $this->path = '42';
        $this->videoStream = new VideoStream($this->disk, $this->path);
    }

    /**
     * {@inheritdoc}
     */
    protected function tearDown(): void
    {
        parent::tearDown();

        unset($this->videoStream);
        unset($this->disk);
        unset($this->path);
    }

    public function testStart(): void
    {
        /**
*
         *
 * @todo This test is incomplete.
*/
        self::markTestIncomplete();
    }
}
